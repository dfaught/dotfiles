"Ack specific bindings and settings
"
"let g:ackprg = 'ag --smart-case --nogroup --nocolor --column'

"switched to Ag but too lazy to make another file.

let g:agprg="ag --column --smart-case"
let g:aghighlight=1

